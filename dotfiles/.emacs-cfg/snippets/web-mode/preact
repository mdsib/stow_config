# -*- mode: snippet -*-
# name: preactbase
# key: preact
# --
import { h, Component} from 'preact';

export default class ${0:`
(yas-choose-value (reverse (remove-if 'string-empty-p
(split-string (file-name-sans-extension (buffer-file-name))
"/"))))
`} extends Component {
render() {
return (
<div>
$1
</div>
);
}
}